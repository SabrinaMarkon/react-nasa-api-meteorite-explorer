import React, { Component } from 'react';

export default class ResultHeadings extends Component {

  render() {
    return (
      <div>
        Name, Id, Name Type, Rec Class, Mass (g),
        Fall, Year, Latitude, Longitude
      </div>
    );
  }
}