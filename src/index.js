import React from 'react';
import ReactDOM from 'react-dom';
import App from './containers/App';

ReactDOM.render(
  <h4>React 16 (the one with hooks) Boilerplate with JSX, Babel, Webpack, React-Router, Redux, and Jest<App /></h4>,
  document.getElementById('root')
);
